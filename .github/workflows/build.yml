name: Build HTTPCustom for BPI-R4

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Install Build Dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y build-essential clang flex bison g++ gawk gcc-multilib g++-multilib gettext git libncurses5-dev libssl-dev python3-setuptools rsync swig unzip zlib1g-dev file wget curl

    - name: Fetch Latest OpenWrt SDK (MediaTek Filogic)
      run: |
        # Core Logic: ดึงชื่อไฟล์ SDK ล่าสุดแบบไดนามิกจากหน้าเว็บ OpenWrt ป้องกันลิงก์ตายเมื่อมีการอัปเดตเวอร์ชัน GCC
        BASE_URL="https://downloads.openwrt.org/snapshots/targets/mediatek/filogic"
        SDK_FILE=$(curl -s "$BASE_URL/sha256sums" | grep "openwrt-sdk" | grep "tar.xz" | awk '{print $2}' | sed 's/*//g')
        
        echo "Downloading $SDK_FILE..."
        wget "$BASE_URL/$SDK_FILE"
        tar -xf openwrt-sdk-*.tar.xz
        mv openwrt-sdk-*/ openwrt-sdk/

    - name: Prepare Package & Update Feeds
      run: |
        # สร้างโฟลเดอร์ package และย้ายโค้ดของเราเข้าไป
        mkdir -p openwrt-sdk/package/luci-app-httpcustom
        # คัดลอกทุกอย่างยกเว้น .github และ openwrt-sdk
        rsync -av --exclude='.github' --exclude='openwrt-sdk' --exclude='*.tar.xz' . openwrt-sdk/package/luci-app-httpcustom/
        
        cd openwrt-sdk
        ./scripts/feeds update -a
        ./scripts/feeds install -a
        make defconfig

    - name: Compile Package
      run: |
        cd openwrt-sdk
        # บังคับ Compile เฉพาะแพ็กเกจของเราเพื่อลดเวลา
        make package/luci-app-httpcustom/compile V=s

    - name: Upload Artifact (.apk)
      uses: actions/upload-artifact@v4
      with:
        name: luci-app-httpcustom-bpi-r4
        path: openwrt-sdk/bin/packages/*/base/luci-app-httpcustom_*.apk
        retention-days: 7
